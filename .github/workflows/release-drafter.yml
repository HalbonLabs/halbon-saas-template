name: Automation - Release Drafter

on:
  push:
    branches: [ main ]
  pull_request_target:
    types: [closed]
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: read

jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - name: Update release draft (push)
        if: github.event_name == 'push'
        uses: release-drafter/release-drafter@b1476f6e6eb133afa41ed8589daba6dc69b4d3f5 # v6 renovate: datasource=github-tags depName=release-drafter/release-drafter
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          config-name: release-drafter.yml
      - name: Update release draft (merged PR)
        if: github.event_name == 'pull_request_target' && github.event.pull_request.merged == true
        uses: release-drafter/release-drafter@b1476f6e6eb133afa41ed8589daba6dc69b4d3f5 # v6 renovate: datasource=github-tags depName=release-drafter/release-drafter
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          config-name: release-drafter.yml
